export type SiteConfig = {
  siteTitle: string;
  siteDesc: string;
  siteUrl: string;
  siteBase: string;
  siteType: string;
  siteLocale: string;
  siteIcon: string;
  siteImg: string;
};

export type HeaderLink = {
  name: string;
  url: string;
};

export type webglCategory = {
  name: string;
  slug: string;
};

export type glslCategory = {
  name: string;
  slug: string;
};
