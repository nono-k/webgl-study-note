import type { Category } from '@/types/config';

export const webglCategoryList: Category[] = [{ name: 'Basic', slug: 'basic' }];

export const glslCategoryList: Category[] = [
  { name: 'Image Processing', slug: 'image-processing' },
];
